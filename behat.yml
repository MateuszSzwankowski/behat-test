default:
    suites:
        my_suite:
            contexts:
                - FeatureContext
    extensions:
        Behat\MinkExtension:
            base_url:  'http://dev-panel.serwis-grupowy-vgp.pl'
            sessions:
                default:
                    selenium2: 
                        capabilities: { "browserName": "chrome", "browser": "chrome", 'chrome': ~}
